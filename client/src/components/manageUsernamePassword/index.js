import React from 'react';
import styled from 'styled-components';

export const ManageUser = ({username, password}) => {   
    return (
        <Container>
            <h2>Manage Your Account</h2>
        </Container>
    );
};

const Container = styled.div`

`;